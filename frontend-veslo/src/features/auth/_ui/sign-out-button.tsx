"use client";

import { Button } from "@/shared/ui/button";
import { useSignOut } from "../_vm/use-sign-out";

export function SignOutButton() {
    //const {isPending, signOut} = useSignOut();

    //return <Button disabled={isPending} variant={'outline'} onClick={() => signOut()}>Sign Out</Button>
}
